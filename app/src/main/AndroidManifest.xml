<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Package Visibility for Android 11+ (API 30+) -->
    <queries>
        <!-- WhatsApp & Variants -->
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />

        <!-- Telegram & Variants -->
        <package android:name="org.telegram.messenger" />
        <package android:name="org.telegram.messenger.web" />
        <package android:name="org.telegram.plus" />
        <package android:name="org.thunderdog.challegram" />
        <package android:name="tw.nekomimi.nekogram" />

        <!-- Eitaa & All Known Variants -->
        <package android:name="ir.eitaa.messenger" />
        <package android:name="ir.eitaa.messenger.web" />
        <package android:name="ir.eitaa.messenger.direct" />
        <package android:name="ir.eitaa.messenger2" />
        <package android:name="ir.eitaa" />
        <package android:name="com.eitaa.messenger" />

        <!-- Bale & Variants -->
        <package android:name="ir.nasim" />
        <package android:name="ir.ble.messenger" />

        <!-- Rubika & Variants -->
        <package android:name="app.rbmain.a" />
        <package android:name="ir.resaneh1.iptv" />
        <package android:name="ir.rubika.chat" />

        <!-- Soroush Plus & Variants -->
        <package android:name="mobi.mmdt.ottplus" />
        <package android:name="mobi.mmdt.ott" />

        <!-- iGap -->
        <package android:name="net.iGap" />

        <!-- Gap -->
        <package android:name="com.gapafzar.messenger" />

        <!-- Deep-link schema queries -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="whatsapp" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="eitaa" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="bale" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="rubika" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="soroush" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="igap" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="gap" />
        </intent>
    </queries>

    <application
        android:name=".DirectChatApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DirectChat">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Handle direct send/view from external apps if sharing a phone or text -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>

</manifest>